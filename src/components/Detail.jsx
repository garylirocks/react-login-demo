import React from 'react';
import PropTypes from 'prop-types';

const Detail = ({ }) => {
  return (
    <div>Detail</div>
  );
};

Detail.defaultProps = {
};

Detail.propTypes = {
};

export default Detail;
